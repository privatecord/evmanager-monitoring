route:
  receiver: default
  group_by:
    - alertname
    - project
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    # Add a real receiver later, for example Discord, Slack, email, Gotify or ntfy.
    webhook_configs: []
